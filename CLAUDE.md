# kart-platform

Read `AGENTS.md` at this repo root now, in full, before doing anything else — it is the actual system instructions for this repository (what it is, rules, reading order, agent definitions, build status). This file exists only because Claude Code auto-loads a file named `CLAUDE.md` specifically; it carries no content of its own so nothing here can drift out of sync with `AGENTS.md`.
