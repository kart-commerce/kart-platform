# kart-platform

Read `docs/AGENT_GUIDE.md` now — it has the full onboarding: what this repo is, reading order for pipeline work, approval-gate rules, and current build status.

This file exists only because Claude Code auto-loads a file named `CLAUDE.md` specifically. The actual content lives in the tool-agnostic file above so other tools (e.g. a future `AGENTS.md` for Codex) can point at the same source without duplicating or drifting from it.
