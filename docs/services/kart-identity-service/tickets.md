---
doc_type: tickets
service: kart-identity-service
status: draft
generated_by: ticket-agent
source: [architecture.md, api-contract.yaml, database-design.md, event-contract.md, design-decisions.md, requirement-spec.md, edge-cases.md — no ddd-model.md exists for this service, see note below]
---

# Tickets: kart-identity-service

Local draft. Not yet created as real GitHub Issues — that requires the target repo to exist (Project Scaffold Agent) and is a separate, explicit step.

**No `ddd-model.md` input for this service (documented precedent, not a gap):** per `docs/services/README.md`'s pipeline table and `api-contract.yaml`/`database-design.md`/`event-contract.md`'s own header notes, the DDD Agent stage has not run for `kart-identity-service` — those three docs, plus `architecture.md` and `design-decisions.md`, were all produced directly from the two docs that **are** fully closed (`requirement-spec.md`, `edge-cases.md` — both `status: approved`, every former blocking Open Question resolved: MFA mechanism/TOTP, session/refresh-token TTLs, refresh-token-reuse token-family response, external-IdP role/group mapping, and the escalated "Federated Login With No Matching Kart Account" edge case), at the orchestrating pipeline's explicit direction. This is the same documented precedent already used for `kart-analytics-service/tickets.md`.

**Approval-checkbox flag (carried forward, not blocking):** `architecture.md` (`pending-approval`), `design-decisions.md` (`proposed`), `api-contract.yaml` (`pending-approval`), `database-design.md` (`pending-approval`), and `event-contract.md` (`pending-approval`) all still carry an unchecked sign-off checkbox. Each of those five docs' own header already states it was produced from the two closed, approved docs plus the not-yet-approved docs above it in the DAG, and none of them carry an open question of their own that would change how the work below is sliced. This ticket list is decomposed directly from all five docs' already-decided content. Re-confirm against those five once a human checks their sign-off boxes — no substantive rework is expected, consistent with how `kart-analytics-service/tickets.md` handled the same situation.

## Epic: kart-identity-service v1

Platform AuthN/token-issuance, session/refresh-token lifecycle, TOTP MFA, RBAC role/claim issuance, and SSO/identity federation (enterprise SAML/OIDC + customer social OIDC) — the platform's single token issuer and sole point of contact with any external IdP (BRD §24.1/§24.2; `architecture.md` Boundary Rationale). Eighteen vertical slices below map 1:1 to `api-contract.yaml`'s eighteen paths; no endpoint in the contract is left undecomposed and no ticket invents a route the contract doesn't already have.

| ID | Task | Vertical Slice (Application/Features/) | Depends On | Design Source |
|---|---|---|---|---|
| IDN-1 | Publish RS256 signing key + JWKS discovery endpoint | `GetJwks` | — | `api-contract.yaml` `GET /.well-known/jwks.json`; `design-decisions.md` "JWT Signing Algorithm" (RS256, asymmetric, private key stays at Identity only) |
| IDN-2 | Register a native (email/password) account | `RegisterUser` | IDN-1 | `api-contract.yaml` `POST /auth/register`; `database-design.md` `users`, `outbox_events`, `sessions`; `event-contract.md` `UserRegistered` (3x retry, `identity.user-registered.dlq`) and `SessionCreated` (2x retry, `identity.session-created.dlq`) |
| IDN-3 | Authenticate with native email/password credentials | `Login` | IDN-1 | `api-contract.yaml` `POST /auth/login`; `edge-cases.md` "Credential Stuffing / Brute-Force" (progressive throttle, 429), "Partial-Auth Window During MFA" (server-side challenge only, no token), "Session Fixation via Pre-Auth Session Reuse" (always regenerate session id); `database-design.md` `users`, `sessions`; `event-contract.md` `SessionCreated` (only on full, non-MFA-pending success) |
| IDN-4 | Begin TOTP MFA enrollment | `EnrollMfa` | IDN-3 | `api-contract.yaml` `POST /auth/mfa/enroll`; requirement-spec.md §2 MFA FR (TOTP, RFC 6238); `database-design.md` `mfa_credentials` (`status = 'pending'`, AES-256-encrypted secret) |
| IDN-5 | Confirm TOTP MFA enrollment | `ConfirmMfaEnrollment` | IDN-4 | `api-contract.yaml` `POST /auth/mfa/enroll/confirm`; `database-design.md` `mfa_credentials` (`pending` → `active`) |
| IDN-6 | Complete a pending MFA challenge and mint tokens | `VerifyMfa` | IDN-3, IDN-5 | `api-contract.yaml` `POST /auth/mfa/verify`; `edge-cases.md` "Partial-Auth Window During MFA"; `database-design.md` `mfa_credentials`, `sessions`; `event-contract.md` `SessionCreated` |
| IDN-7 | Issue a Client Credentials service-principal token | `IssueServicePrincipalToken` | IDN-1 | `api-contract.yaml` `POST /auth/token`; requirement-spec.md §2 (OAuth2 Client Credentials flow); `database-design.md` `service_principals` |
| IDN-8 | Rotate a refresh token (with reuse-family revocation + concurrency control) | `RefreshToken` | IDN-3 | `api-contract.yaml` `POST /auth/refresh`; `edge-cases.md` "Refresh Token Replay After Rotation" (token-family reuse detection), "Concurrent Refresh Race" (DB-level conditional update); `design-decisions.md` "Concurrency Control for Refresh-Token Rotation"; `database-design.md` `refresh_tokens` (`parent_token_id` lineage, `uq_refresh_tokens_hash`), `sessions.revoked_reason = 'reuse_detected'` |
| IDN-9 | Log out (revoke current session's tokens) | `Logout` | IDN-3 | `api-contract.yaml` `POST /auth/logout`; `edge-cases.md` "Stale Revocation Under Stateless JWT Validation"; `design-decisions.md` "Shared State-Store Technology for Ephemeral Security State" (Redis revocation list, `identity:revocation:*`); `database-design.md` `sessions.revoked_reason = 'logout'` |
| IDN-10 | Lock a user's ability to authenticate (admin-triggered) | `LockUser` | IDN-7 | `api-contract.yaml` `POST /internal/users/{userId}/lock`; requirement-spec.md §2 admin-suspension FR; ADR-0010; `database-design.md` `users.locked_at`, `idx_sessions_user_live` (revoke every live session) |
| IDN-11 | Unlock a previously locked user | `UnlockUser` | IDN-10 | `api-contract.yaml` `POST /internal/users/{userId}/unlock`; ADR-0010 |
| IDN-12 | Initiate a native-account password reset | `InitiatePasswordReset` | IDN-2 | `api-contract.yaml` `POST /auth/password/reset-initiate`; `database-design.md` `password_reset_tokens` |
| IDN-13 | Confirm a password reset (+ revoke all outstanding sessions) | `ConfirmPasswordReset` | IDN-12 | `api-contract.yaml` `POST /auth/password/reset-confirm`; `database-design.md` `password_reset_tokens`, `sessions.revoked_reason = 'password_reset'` |
| IDN-14 | Start enterprise SSO federation (SP-initiated redirect) | `StartEnterpriseFederation` | IDN-1 | `api-contract.yaml` `GET /auth/sso/enterprise/{idpAlias}/login`; `design-decisions.md` "Resilience Pattern for External IdP Calls" (per-IdP circuit breaker/bulkhead/timeout budget) |
| IDN-15 | Enterprise SAML Assertion Consumer Service (ACS) | `EnterpriseSamlAssertionConsumer` | IDN-14 | `api-contract.yaml` `POST /auth/sso/enterprise/{idpAlias}/saml/acs`; `edge-cases.md` "SAML Assertion Replay at the ACS Endpoint" (consumed-assertion-ID cache), "Federated Login With No Matching Kart Account" (JIT provisioning); requirement-spec.md §2 resolved Q7 (fail-closed IdP-group→role mapping, `Admin` never auto-granted); `database-design.md` `federated_identities`, `idp_group_role_mappings`, `users`, `sessions` (24h absolute cap, no sliding) |
| IDN-16 | Enterprise OIDC federation callback | `EnterpriseOidcCallback` | IDN-14 | `api-contract.yaml` `GET /auth/sso/enterprise/{idpAlias}/oidc/callback`; same role-resolution/JIT-provisioning/fail-closed-mapping decisions as IDN-15 |
| IDN-17 | Start customer social login redirect | `StartSocialLogin` | IDN-1 | `api-contract.yaml` `GET /auth/sso/social/{provider}/login` |
| IDN-18 | Customer social login OIDC callback (Customer-only role, JIT provisioning) | `SocialLoginCallback` | IDN-17 | `api-contract.yaml` `GET /auth/sso/social/{provider}/callback`; requirement-spec.md §2 resolved Q7 (social login always resolves to `Customer` only, never elevated); `edge-cases.md` JIT provisioning; `database-design.md` `federated_identities`, `users`, `user_roles` (`'customer'` row) |

## Flagged Gaps — Not Decomposed Into Tickets (would invent scope, not decompose it)

- **No contracted endpoint changes login email/display name after registration.** requirement-spec.md §2 and Domain Invariant §4 (ADR-0006) require Identity to publish `UserAccountUpdated` "whenever Identity's copy of login email or display name changes — not just at registration time," but no endpoint anywhere in `api-contract.yaml` writes to `users.email`/`users.display_name` post-registration (the only writers are `POST /auth/register` at creation time and the password-reset pair, which touches `password_hash` only). This is a genuine gap between a stated FR/invariant and the contracted API surface that none of `architecture.md`, `database-design.md`, or `event-contract.md` flagged explicitly (unlike the two gaps below, which they did flag) — surfaced here instead of inventing a route. No ticket exists for triggering `UserAccountUpdated`'s publish side; one should be added once an API Design Agent pass (or human) decides the endpoint shape.
- **No public endpoint for native role elevation (grant `support_agent`/`admin` on a `user_roles` row).** `database-design.md` itself flags this ("an out-of-band administrative process no stated FR defines a public endpoint for") — no ticket, consistent with that flag.
- **No public endpoint for managing `idp_group_role_mappings`.** `database-design.md` itself flags this ("written by an out-of-band operator process; no endpoint for it exists yet... flagged here as a gap for a later API Design Agent pass") — no ticket, consistent with that flag.
- **RBAC-role-change-triggers-revocation enforcement** (`edge-cases.md`, "RBAC Role Change Outlives an Already-Minted JWT") has no ticket of its own because there is no role-change endpoint to trigger it (see the role-elevation gap above) — the mechanism itself (extend the Redis revocation list IDN-9/IDN-10 already write to) requires no new infrastructure once a role-change endpoint exists; wiring it in is a small addition to whichever future ticket implements that endpoint, not a standalone task today.
- **Gateway-side revocation-list consumption is out of scope for this service's own ticket list.** `architecture.md`'s Dependencies table states the API Gateway reads Identity-owned Redis keys directly as shared infrastructure, not via an API/event call — that read-side implementation belongs in the Gateway's own repo/ticket list. IDN-9, IDN-10, and IDN-13 above are responsible only for the write side (populating the revocation list).
- **Federation Single Logout / SLO propagation** — requirement-spec.md §6 explicitly carries this forward as a non-blocking, later-pipeline-stage item, and `architecture.md`'s "Sync vs. Async Resolution" section confirms no SLO webhook/SCIM integration is added to Identity's boundary in this pass (the 24-hour federated-refresh-token absolute cap, built as part of IDN-15/IDN-16, is the accepted v1 mitigation). No ticket — correctly out of v1 scope per both docs.
- **`UserDataErased` (ADR-0016) consumption for Identity's own PII** (login email, password hash, encrypted TOTP secret) — `architecture.md`'s "Flagged for Human Review" section and `database-design.md`'s and `event-contract.md`'s matching flags all explicitly decline to resolve this unilaterally, since ADR-0016's Consequences list doesn't name Identity. No ticket until a human confirms whether Identity needs a consumer/redaction handler for this event.

## Notes for Sprint Planner Agent

- IDN-1 (`GetJwks` / RS256 key provisioning) is the single foundation task — every token-issuing slice (IDN-2, IDN-3, IDN-7, IDN-14, IDN-17) depends on it directly, and everything else depends on one of those transitively. Start it first, alone.
- IDN-15 (`EnterpriseSamlAssertionConsumer`) and IDN-16 (`EnterpriseOidcCallback`) share the same underlying JIT-provisioning + fail-closed IdP-group→role-mapping domain logic (requirement-spec.md §2, resolved Q7) but are independent REST entry points with no lifecycle dependency on each other — recommend building IDN-15 first as the reference implementation (BRD §24.2 names SAML before OIDC) and having IDN-16 reuse its domain logic, but this is a sequencing recommendation, not a hard dependency edge.
- IDN-4 → IDN-5 → IDN-6 is this graph's longest chain from IDN-3 (enroll → confirm → verify-at-login), 3 deep; combined with IDN-1 → IDN-3 at the front, the longest chain overall is 5 nodes (IDN-1 → IDN-3 → IDN-4 → IDN-5 → IDN-6).
- IDN-10 → IDN-11 (lock → unlock) and IDN-12 → IDN-13 (reset-initiate → reset-confirm) are natural sequential pairs, mirroring `kart-offer-service/tickets.md`'s OFF-5/OFF-6 activate/deactivate precedent.
- IDN-8 (`RefreshToken`), IDN-9 (`Logout`), IDN-12 (`InitiatePasswordReset` via IDN-2), IDN-14 (`StartEnterpriseFederation`), and IDN-17 (`StartSocialLogin`) each depend on only one prior task and can otherwise be parallelized once their single dependency lands.
- No circular dependencies in this graph.
