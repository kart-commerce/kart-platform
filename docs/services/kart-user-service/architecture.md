---
doc_type: architecture
service: kart-user-service
status: approved
generated_by: architecture-agent
source: docs/services/kart-user-service/requirement-spec.md, docs/services/kart-user-service/edge-cases.md, docs/services/kart-user-service/design-decisions.md
---

# Architecture: kart-user-service

## Boundary Rationale

`kart-user-service` is the bounded context that answers "what does this user's profile look like" — addresses, preferences, and a denormalized display copy of a couple of Identity-owned fields — never "who is this and are they allowed to do X," which stays Identity's alone (BRD §2.1 item 2 vs. item 1; requirement-spec.md §1). The boundary between the two is not this service's own call to make and is not re-litigated here: [ADR-0006](../../adr/0006-identity-user-profile-sync-event.md) already settled it platform-wide — Identity owns login-email/display-name as source of truth and publishes `UserAccountUpdated` on change; User owns the broader profile (addresses, preferences) and keeps a denormalized, eventually-consistent copy of those two fields, reconciled via that event. `kart-identity-service/architecture.md`'s own Boundary Rationale states the identical split from its side ("Identity never models a full user profile, and User never re-derives or overrides Identity's copy of login email/display name") — the two docs agree, no divergence to flag.

User Service is a CQRS-split service internally (PostgreSQL write model, MongoDB read projection via the platform's standard Outbox pipeline — requirement-spec.md §2, §3, §7), but that split is an internal implementation detail, not a boundary concern: both stores are owned entirely by this one service, and no other service ever writes to either directly.

User Service is absent from the Order Saga (BRD §12) and from BRD §5.5's boundary diagram — requirement-spec.md §3/§6 item 7 already committed it to the platform's secondary availability tier (99.9%) on that basis. Nothing else on the platform synchronously blocks on User Service being up, and (see Dependencies below) User Service itself holds no synchronous outbound dependency on any other service — the least-coupled boundary shape a non-Saga, profile-holding service can have.

The GDPR/erasure policy (ADR-0016, `docs/adr/0016-user-gdpr-erasure-policy.md`) is likewise cited, not re-decided, here: PII is tombstoned in the write model within 30 days of a verified request, retained order/audit history elsewhere on the platform is anonymized rather than deleted, and a new `UserDataErased` event fans the erasure out to the six named downstream consumers. This document's job is only to place that already-decided policy into the dependency graph (see Dependencies) and to resolve the one piece it genuinely left open — the erasure-request intake mechanism (see the dedicated section below).

## Dependencies

| Direction | Peer Service | Mechanism | Type | Notes |
|---|---|---|---|---|
| Inbound (client) | Client/Storefront, via API Gateway | `/users/{id}` (verb/read-write split not yet fixed) | **Sync** (REST) | Gateway-routed like any other client-facing endpoint — resolves requirement-spec.md §5/§6 item 2 (the Service Boundary Diagram's omission of a `GW → User` edge is non-exhaustive, not a routing exclusion). Final verb split remains the API Design Agent's job (requirement-spec.md §7) |
| Inbound (consumed) | Identity | `UserRegistered` | **Async** | Seeds profile creation (requirement-spec.md §2, §4); idempotent by upsert-on-`userId` (edge-cases.md, "Duplicate/out-of-order `UserRegistered` delivery"); 3x retry, `identity.dlq` (ADR-0007) |
| Inbound (consumed) | Identity | `UserAccountUpdated` (payload `userId`, `email`, `displayName`, `updatedAt`) | **Async** | Reconciles User's denormalized email/display-name copy (ADR-0006); 2x retry, own scoped DLQ (ADR-0007/ADR-0008; see event-contract.md). `updatedAt` is a confirmed, additive schema field per `kart-identity-service/event-contract.md` (approved) — this consumer applies an incoming update only if `updatedAt` is strictly newer than its currently-stored value, resolving the ordering gap an earlier drafting pass of this document had left open (see Distributed-Monolith Risk) |
| Inbound | Admin Service | `POST /internal/users/{userId}/erasure-requests` (indicative name; final route is the API Design Agent's job) | **Sync** (internal REST, client-credentials-gated) | Triggers the ADR-0016 tombstone write. Caller resolved by **[ADR-0017](../../adr/0017-user-erasure-request-intake-caller.md)**, added as a fifth business category ("compliance/erasure request intake") to ADR-0010's closed enumeration, exercised via the BRD's existing `Support Agent` role — the "erasure-request intake / trigger mechanism" that `design-decisions.md` explicitly left to this stage ("Not Decided Here," item 5) is now fully closed, mechanism and caller both |
| Outbound (published) | Analytics | `UserProfileUpdated` | **Async** | Full fan-in default (ADR-0004); row added to the Event Catalog by ADR-0008; 2x retry, `user.dlq` (ADR-0007/ADR-0008) |
| Outbound (published) | Notification | `UserNotificationPreferenceUpdated` (payload: `userId`, per-channel-per-category opt-out map, `appInstalled` boolean) | **Async** | New publish responsibility for User Service, decided from Notification's side in `kart-notification-service/architecture.md`'s "Resolved Integration-Contract Question" section (closing that service's requirement-spec.md §6 Q3) — not yet reflected in this service's own requirement-spec.md §5 API Surface table; confirmed here as a real dependency edge, with the matching requirement-spec row flagged as a non-blocking documentation-sync follow-up, the same pattern already used elsewhere in this service's docs for `UserAccountUpdated`/`UserDataErased`. Tier 3 retry, non-paged (per Notification's architecture doc) |
| Outbound (published) | Order, Notification, Analytics, Review, Recommendation, Wishlist | `UserDataErased` (payload: `userId`, `erasedAt`) | **Async** | Fan-out of the erasure tombstone (ADR-0016 items 4-6); compliance-critical retry tier — high retry budget + human paging on final DLQ landing, the same posture as Payment's money-moving events, distinct from the standard/looser tier the three events above use (ADR-0016 item 7; design-decisions.md, "Resilience Pattern — Retry/DLQ Tier Classification"). Per ADR-0016's own Consequences section, each named downstream service still needs to add its own consumer to its own requirement-spec/edge-cases in a future pass — not yet true of any of the six today; flagged there, not re-flagged here as if it were a gap in this service's own boundary |

No synchronous outbound dependency on any other service — the one new synchronous edge this stage adds (`POST /internal/users/{userId}/erasure-requests`) is **inbound**; User Service is the callee, never the caller, of any other peer.

## Resolved Integration-Contract Question: Erasure-Request Intake Mechanism and Caller (design-decisions.md, "Not Decided Here," item 5)

`design-decisions.md` correctly deferred this rather than inventing a caller/topology at the design-pattern stage ("not enough grounding here to decide without inventing the caller/topology — left to the Architecture Agent"). Both the mechanism and the caller are now resolved — the latter via ADR-0017, closing what this document's earlier drafting pass had left as a genuine open item:

- **Mechanism decided: a synchronous internal REST call, not an event.** A verified erasure request needs a request-time confirmation that User Service has accepted the tombstone obligation — an acknowledged compliance commitment, not a best-effort fire-and-forget — the same reasoning `kart-admin-service/architecture.md`'s "Sync vs. Async Resolution" already applies to its own audited back-office actions: a write with a compliance/audit consequence should be confirmed synchronously before the caller treats the request as accepted, not learned about later through an event that might land in a DLQ. An async "erasure requested" event was considered and rejected on this basis.
- **Caller: resolved — Admin Service, per [ADR-0017](../../adr/0017-user-erasure-request-intake-caller.md).** The platform's one existing precedent for a back-office actor calling a user-scoped internal endpoint was Admin Service's `POST /internal/users/{userId}/lock`/`unlock` (ADR-0010), whose scope was fixed to exactly four categories — this was a genuine cross-cutting gap (it also determines what `kart-admin-service`'s own docs must expose), not something this document could decide unilaterally for a service it wasn't profiling. ADR-0017 closes it: Admin Service's category enumeration gains a fifth business category ("compliance/erasure request intake"), exercised via the BRD's existing `Support Agent` role, calling this endpoint the same way it already calls Product/Category/Offer/Identity/Inventory for its other four categories. Identity Service was considered and rejected as the caller (its own boundary is authentication/session lifecycle, and it is already a `UserDataErased` *consumer*, not a natural trigger of its own fan-out) — see ADR-0017 for the full reasoning.
- **What is decided, independent of who the caller turned out to be:** the endpoint is internal-only (never reachable via the public API Gateway), client-credentials-gated, and its sole effect is to start the already-decided ADR-0016 tombstone write / 30-day SLA / `UserDataErased` fan-out — no new domain behavior beyond what ADR-0016 already specifies.

## Distributed-Monolith Risk

**None identified at the synchronous-call-graph level.** User Service has zero synchronous outbound dependencies on any other service; its only synchronous edges are both inbound (the client-facing `/users/{id}` surface and the new internal erasure-request intake call), and neither chains into a further synchronous call — a request into User Service never fans out synchronously to Identity, Order, or any other peer. This matches the "foundational-but-decoupled" shape requirement-spec.md's secondary-tier availability posture already implies, not the "chatty, should-be-async" pattern this stage exists to catch.

One risk worth restating here — already accepted elsewhere, not newly introduced by this document, but now visible as a real edge in a dependency-graph-complete picture rather than a single-service one:

- **Downstream `UserDataErased` consumption is fan-out, not fan-in, and its completeness depends on six other services' own future docs passes**, not on anything User Service can enforce from its own boundary. If Order/Notification/Analytics/Review/Recommendation/Wishlist never add their own consumer, ADR-0016's guarantee only holds for User Service's own copy of a user's PII, not the platform's — an accepted risk already named in ADR-0016's own Consequences section, restated here only because it is now a real fan-out edge in this service's own dependency graph rather than an abstract future concern.

**The `UserAccountUpdated` ordering gap is now resolved, not open.** `kart-identity-service/event-contract.md` (approved) confirms the proposed `updatedAt` field as a genuine, additive schema addition — payload is `userId`, `email`, `displayName`, `updatedAt` — and fixes the consumption rule: User Service's consumer applies an incoming update only if `updatedAt` is strictly newer than its currently-stored value for that `userId`. This document adopts that confirmed schema directly (see event-contract.md) rather than re-deciding it.

## Sign-off

- [x] Reviewed by: Automated architecture pipeline — autonomous completion authorized by project owner
- [x] Approved to proceed to DDD Agent
